import React from "react";




 export const BlogPagination = ({page, totalBlogs, paginate,}) => {



  function refreshPage() {
    window.location.reload(false);
  }
  return (
    <div>

    </div>
  );
};

export default BlogPagination
